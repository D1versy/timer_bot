#!/bin/bash
# Скрипт для запуска бота

cd "$(dirname "$0")"

echo "🚀 Запуск L2M Boss Tracker Bot..."

# Проверка виртуального окружения
if [ ! -d "venv" ]; then
    echo "❌ Виртуальное окружение не найдено. Создайте его: python3 -m venv venv"
    exit 1
fi

# Проверка .env
if [ ! -f ".env" ]; then
    echo "❌ Файл .env не найден. Создайте его и добавьте BOT_TOKEN"
    exit 1
fi

# Активация виртуального окружения
source venv/bin/activate

# Проверка зависимостей
echo "📦 Проверка зависимостей..."
pip install -q -r requirements.txt

# Проверка БД
if [ ! -f "app.db" ]; then
    echo "⚠️ База данных не найдена. Создаю..."
    python app/seed.py
fi

# Запуск бота
echo "✅ Запуск бота..."
python bot.py
